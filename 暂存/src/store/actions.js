import {LOGIN_IFRAME_URL} from './mutation-types'

import {
  getLoginUrl_first,
  getLonin_first,
  getLoginUrl_second
} from '../api/index'

export default {
  // async getLoginUrl({commit}) {
  //   const firstQuestUrl = await getLoginUrl_first();
  //   if (firstQuestUrl) {
  //     const firstQuest=await getLonin_first(firstQuestUrl);
  //
  //     // const data = first.data;
  //     // commit(LOGIN_IFRAME_URL, {data})
  //   }
  // },
}
